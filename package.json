{
  "name": "doggoneclean-site",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "check": "python3 scripts/check.py",
    "build": "python3 scripts/check.py && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.13.0"
  }
}
