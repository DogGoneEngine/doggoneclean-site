---
// src/pages/orbit.astro
// Alias for Laelaps at /orbit, kept for old bookmarks and the Google sign-in
// redirect. The console is named Laelaps and lives at /laelaps (primary);
// /admin is the other alias. All three mount the same AdminApp.

import AdminLayout from '../layouts/AdminLayout.astro';
import AdminApp from '../components/admin/AdminApp.jsx';
---

<AdminLayout>
  <AdminApp client:load />
</AdminLayout>
